---
description: Safe deploy rules for van2 Customer — Firestore canonical hub
globs: "**/firestore.rules,**/firebase.json,**/scripts/deploy*.ps1,**/functions/index.js"
alwaysApply: false
---

# van2 Customer — Canonical Firestore + functions van2

Docs: `scripts\DEPLOY_GOVERNANCE.md` + `scripts\DEPLOY_RISK_MATRIX.md`

## สิทธิ์ van2

- **SHARED:** Firestore `(default)` rules → กระทบ van1, van3 (ไรเดอร์), van4
- **SELF:** storage, hosting, functions van2 (ทีละชื่อ)

```powershell
scripts\deploy-self.ps1 -App van2 -Target firestore -ConfirmDeploy "APPROVE:van2:van-merchant" -ConfirmImpact "SHARED:van1,van2,van3,van4" -FinalAcknowledge "YES I UNDERSTAND"
```

Manifest: `scripts\deploy-governance.ps1`
