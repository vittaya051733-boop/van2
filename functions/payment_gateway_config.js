/**
 * Mirror ของ lib/config/payment_gateway_config.dart
 * อัปเดตคู่กันเมื่อสลับช่องทางชำระเงิน
 */
const OMISE_GATEWAY_ENABLED = false;

const EMBEDDED_PROMPTPAY_CHECKOUT_ENABLED = true;

module.exports = {
  OMISE_GATEWAY_ENABLED,
  EMBEDDED_PROMPTPAY_CHECKOUT_ENABLED,
};
